import React from "react";

function TodoList() {
  return (
    <div>
        <a href="google.com">Enough already, play the hits!</a>
        <h2>The classic TODO list</h2>
        <p>Let's take a look at the TODO list, a real world example that answers the age old question: what do I need and what do I have.</p>
       
    </div>
  );
}

export default TodoList;
